[package]
name = "vitem-visualize"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "dylib"]

[dependencies]
# ranim.workspace = true
ranim = { version = "0.1.5", features = ["preview"] }
itertools.workspace = true
